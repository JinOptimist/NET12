@model List<ImageViewModel>

@section Scripts{
    <script src="~/js/gallery.js"></script>
}

@*<div class="whole-page">

        <div class="text-center">
            <h1>Users gallery</h1>

            <div class="pic-to-gallery">
                @foreach (var image in Model)
                {
                    <div>
                        <div>
                            <img class="image" src="@image.Picture">
                        </div>
                        <div class="t-to-gallery">
                            @image.Assessment
                        </div>
                        <div class="t-to-gallery">
                            @image.Description
                        </div>
                        @if (User.Identity.IsAuthenticated)
                        {
                            <div>
                                <a href="@Url.Action("Wonderful","Gallery", new {@imageId = image.Id})" class="good-button">Wonderful</a>
                                <button class="bad-button">Awful</button>
                            </div>
                        }

                    </div>

                }
            </div>
        </div>
    </div>*@

<div class="gallery-name">
    <span>Users gallery(carousel)</span>
</div>

<div class="carousel-container">

    <div class="left-button">
        <button class="l-button"><span>Left</span></button>
    </div>

    <div class="img-container">
        @for (var i = 0; i < Model.Count; i++)
        {
            <div class="pic-item" index="@i">
                <img src="@Model[i].Picture">
            </div>
        }
    </div>

    <div class="right-button">
        <button class="r-button"><span>Rigft</span></button>
    </div>

</div>

